import 'package:flutter/material.dart';

class AppColors {
  // Primary color for the app (e.g., for buttons, active elements)
  static const Color primaryColor = Colors.grey;

  // Background color for the app (dark theme)
  static const Color backgroundColor = Colors.white;

  // Text color (light text on dark backgrounds)
  static const Color textColor = Colors.black;

  // Drawer header color (a muted color to contrast with the dark theme)
  static const Color drawerHeaderColor = Color(0xECBF7BA);

  // Bottom navigation bar item color (inactive item color)
  static const Color bottomNavigationBarItemColor = Colors.grey;

  // Bottom navigation bar item selected color (highlight active item)
  static const Color bottomNavigationBarItemSelectedColor = Colors.black;

  // Bottom navigation bar item background color (for dark theme)
  static Color bottomNavigationBarItemBackgroundColor = Colors.grey.shade800;

  // Divider color (used between elements or lists)
  static Color dividerColor = Colors.grey.shade800;

  // Button color (matches the primary color for call-to-action buttons)
  static const Color buttonColor = Colors.deepPurple;

  // Button text color (light text on buttons)
  static const Color buttonTextColor = Colors.white;

  // Caption text color (used for secondary or less important text)
  static const Color captionColor = Colors.grey;

  // Error color (for showing errors)
  static const Color errorColor = Colors.red;

  // Success color (for showing success messages or indicators)
  static const Color successColor = Colors.green;

  // Warning color (for warning messages)
  static const Color warningColor = Colors.orange;

  // Information color (for informational messages)
  static const Color infoColor = Colors.blue;

  // Input field background color (for dark input fields)
  static Color inputBackgroundColor = Colors.grey.shade800;

  // Input field text color (light text in input fields)
  static const Color inputTextColor = Colors.white;

  // Input field hint text color (muted color for placeholder text)
  static const Color inputHintTextColor = Colors.grey;

  // Input field border color (for focused and unfocused borders)
  static Color inputBorderColor = Colors.grey.shade800;
}
